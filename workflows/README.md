# n8n Workflows

Place exported n8n workflow JSON files here for backup/reference.

To use them:

```text
n8n → Workflows → Import from File
```

These files are not automatically imported just by deploying n8n. You must import them inside the n8n UI.
